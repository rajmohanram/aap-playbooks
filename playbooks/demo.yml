- name: Sample playbook - Hello World, AWX
  hosts: localhost
  connection: local
  gather_facts: no
  vars:
    message: "Hello World, AWX"
  
  tasks:
    - name: Print Hello World
      debug:
        var: message

    - name: Debug vars
      debug:
        var: vars
